apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-nfs-development-light-data-xxx
  # name: nfs-in-pvc
spec:
  storageClassName: nfs-development-light-data-xxx
  # volumeName: nfs-development-light-data-xxx
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
  # storageClassName: ""  # если используешь стандартный StorageClass, можно убрать
